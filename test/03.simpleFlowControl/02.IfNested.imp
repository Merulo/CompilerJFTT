DECLARE
  a; b;
IN
  IF 0 > 1 THEN
    IF 2 > 3 THEN
      IF 4 > 5 THEN
        IF 6 > 7 THEN
          a:= 100;
        ENDIF
      ENDIF
    ENDIF
    IF 8 > 9 THEN
      a:= 200;
    ENDIF
    IF 10 > 11 THEN
      a:= 300;
    ENDIF
    IF 12 > 13 THEN
      a:= 400;
    ENDIF
    a := 500;       
  ENDIF
  a := 600;
END